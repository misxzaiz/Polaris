# 终端

Polaris 内置终端面板，基于 xterm.js，支持命令行操作。

通过 ActivityBar 的 **Terminal** 图标打开终端面板。

## 功能

| 功能 | 说明 |
|------|------|
| 命令执行 | 在终端中执行任意 Shell 命令 |
| 多行输出 | 支持长输出滚动查看 |
| 自适应尺寸 | 面板宽度变化时自动调整终端列数 |

终端面板使用系统默认 Shell（Windows 为 PowerShell/CMD，macOS 为 zsh/bash）。
