# 任务队列

## 待办
1. 评估 UI 改造点：梳理 `TaskEditor`、`SchedulerPanel`、`schedulerStore` 在协议模式下需要新增的配置项与反馈展示。

## 已完成
- [x] 分析任务目标：探索定时任务改造方向（完成首轮现状调研，进度推进到 10%）
- [x] 评估“模板单一数据源 + 协议状态结构化 + 执行反馈闭环”三阶段改造方案的落地顺序与风险（确认优先顺序为模板统一 → 状态结构化 → 执行闭环/UI 反馈，进度推进到 20%）
- [x] 对比模板单一数据源三种实现路径：Rust 持有内置模板、前端下发模板快照、共享模板定义生成前后端产物，并给出推荐顺序与迁移策略（推荐首步以“前端模板快照唯一运行时输入”止血，再演进到共享模板资产，进度推进到 30%）
- [x] 设计协议任务状态模型：确认应把“进度、完成项、当前焦点、待办选择结果、补充处理状态、文档统计”提升为结构化协议状态，并把单次执行产生的选择/归档/回写结果沉淀到运行结果而非仅留在 Markdown（进度推进到 40%）
- [x] 设计执行闭环：明确 scheduler 在执行前后对 user-supplement、memory/index、memory/tasks 的读写职责，减少仅靠 AI 自行维护协议状态的不确定性（确认需要引入 protocol runtime snapshot/result，并把 supplement 归档时机调整为“结构化结果落地之后”，进度推进到 50%）
